apiVersion: v1
kind: ConfigMap
metadata:
  name: smcprober-config
  namespace: "{{ .Values.namespace }}"
  labels: {{- include "helm.labels" . | nindent 4 }}
data:
  config.json: |
{{ .Values.configJSON | indent 4 }}
  configExporter.json: |
{{ .Values.configExporterJSON | indent 4 }}
