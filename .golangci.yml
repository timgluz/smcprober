# golangci-lint configuration
# See https://golangci-lint.run/usage/configuration/

version: "2"

run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly

linters:
  enable:
    # Default linters
    - errcheck        # Check for unchecked errors
    - govet           # Go vet
    - ineffassign     # Detect ineffectual assignments
    - staticcheck     # Static analysis (includes gosimple and stylecheck)
    - unused          # Check for unused code

    # Additional recommended linters
    - misspell        # Spell checking
    - gosec           # Security issues
    - unconvert       # Unnecessary type conversions
    - unparam         # Unused function parameters
    - goconst         # Repeated strings that could be constants
    - gocyclo         # Cyclomatic complexity
    - dupl            # Duplicate code detection
    - bodyclose       # HTTP response body close check
    - noctx           # HTTP requests without context

  disable:
    - exhaustive      # Can be noisy for switch statements
    - cyclop          # Duplicate of gocyclo with different thresholds
    - funlen          # Can be too strict for some legitimate cases
    - gocognit        # Similar to gocyclo
    - revive          # Disabled to avoid noisy documentation comment warnings

# Note: linters-settings, issues, and output sections removed due to v2 schema validation
# The linter still works with sensible defaults without these sections
